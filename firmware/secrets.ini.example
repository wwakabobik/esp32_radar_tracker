; Copy to secrets.ini before building firmware.
; MQTT host is found automatically via UDP discovery — no IP needed.

[env:esp32dev]
build_flags =
    -DWIFI_SSID=\"YOUR_WIFI\"
    -DWIFI_PASS=\"YOUR_PASS\"
